# The OPAC is the public catalogue. Public ON PURPOSE.
#
# Declared explicitly rather than left to inherit, so the intent is visible and
# an audit can tell "deliberately public" apart from "nobody wrote a file".

all:
  is_secure: false
