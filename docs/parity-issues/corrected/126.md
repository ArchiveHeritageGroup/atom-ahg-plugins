**CORRECTED** (cross-plugin re-verification, 2026-06-01). The first same-name audit over-counted; this reflects the true gap.

**Domain:** rights-privacy-compliance — verified: **0 missing, 5 partial, 5 already-present (false positives dropped).**

### Genuinely missing
- (none)

### Partial (real sub-gap remains)
- WebAuthn/FIDO2 passkey MFA (issue #721) — TOTP (RFC 6238) two-factor auth fully implemented via TotpService in atom-framework/src/Core/Security/TotpService.php (Google Authenticator, Authy compatible). 
- DPIA with risk scoring — DPIA framework present with 'requiring_dpia' flag in ROPA. Risk scoring visible in piiScanObjectSuccess.blade.php (risk_score >= 70 → 'bg-danger'). No evidence 
- Audit trail chaining (cryptographic verification) — Audit trail tables (ahg_audit_log, ahg_audit_access, ahg_audit_authentication) capture all actions with UUID for deduplication. No cryptographic hash chaining, 
- MFA policy enforcement per role (issue #738) — Two-factor auth enforcement EXISTS but only at clearance-level (two_factor_verified in user_security_clearance). No evidence of per-role MFA policy matrix (e.g.
- PII scan & redaction review workflow — PII detection and review queue fully functional: piiScanPiiTask scans records, piiReviewSuccess.blade.php shows approval/redaction buttons. ahg_pii_finding_embe

### Already present in PSIS (was a false positive — no work)
- Visual redaction editor for privacy (images/PDF) — ahgPrivacyPlugin/lib/Service/VisualRedactionService.php + modules/privacyAdmin/a
- Embedded metadata PII scanning — ahgPrivacyPlugin/lib/Service/EmbeddedMetadataPiiService.php + database/migration
- Article 30 ROPA export (GDPR compliance) — ahgPrivacyPlugin/lib/Extensions/Privacy/Controllers/PrivacyComplianceController.
- Retention schedule + disposal workflow (records management) — ahgExtendedRightsPlugin/lib/Services/DisposalWorkflowService.php + database/migr
- Watermark application to derivatives (security classification) — ahgSecurityClearancePlugin/lib/Services/WatermarkService.php + Commands/Watermar

Full report: docs/HERATIO_PSIS_PARITY_AUDIT.md + corrected verdicts in this session's log.