**CORRECTED** (cross-plugin re-verification, 2026-06-01). The first same-name audit over-counted; this reflects the true gap.

**Domain:** workflow-reporting-misc — verified: **1 missing, 1 partial, 4 already-present (false positives dropped).**

### Genuinely missing
- **Observability infrastructure (Prometheus metrics, tracing, APCu/Redis metrics storage)** — No Prometheus exporter, metrics registry, or observability service found anywhere in atom-ahg-plugins or atom-framework. Documentation acknowledges ahg-observab

### Partial (real sub-gap remains)
- Console command: WorkflowNotifyOverdueCommand for scheduled overdue notifications — The command exists as 'workflow:process' with --notifications flag which processes pending notifications (including overdue via queueSlaNotification in Workflow

### Already present in PSIS (was a false positive — no work)
- SLA policies with escalation actions and warning thresholds — ahgWorkflowPlugin/lib/Services/WorkflowSlaService.php + database/install.sql (ah
- Mailable notification classes for workflow tasks (Approved, Rejected, Overdue) — ahgWorkflowPlugin/lib/Services/WorkflowService.php + database/install.sql (ahg_w
- ReportBuilder service layer with template/section/binding management — ahgReportBuilderPlugin/lib/ - ReportBuilderService.php, TemplateService.php, Sec
- Heritage admin layer (contributor marketplace, analytics, embargoes) — ahgHeritagePlugin/modules/heritage/actions/actions.class.php + templates/

Full report: docs/HERATIO_PSIS_PARITY_AUDIT.md + corrected verdicts in this session's log.