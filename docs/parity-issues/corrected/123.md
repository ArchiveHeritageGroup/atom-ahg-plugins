**CORRECTED** (cross-plugin re-verification, 2026-06-01). The first same-name audit over-counted; this reflects the true gap.

**Domain:** search-discovery — verified: **3 missing, 2 partial, 7 already-present (false positives dropped).**

### Genuinely missing
- **ResourceSync 1.1 Source endpoint** — No ResourceSync implementation found anywhere in atom-ahg-plugins or atom-framework. Confirmed via grep searches for 'ResourceSync' across entire codebase yield
- **Z3950 server mode (incoming client queries)** — Only Z39.50 CLIENT implementation exists (Z3950Service for searching remote targets via copyCataloguing workflow). No Z39.50 SERVER mode found — PSIS cannot rec
- **Discovery simulated query corpus** — No 'corpus' of pre-built simulated test queries found. ahg_pageindex_query_log table exists to log real user queries, but no corpus.sql or fixture data for test

### Partial (real sub-gap remains)
- Search analytics dashboard with CTR tracking — Search query logging exists (ahg_semantic_search_log table with original_query, expanded_query, result_count, search_time_ms, user_id), but there is NO click-th
- Vector search with Qdrant + RRF blending — Vector search with Qdrant is implemented (VectorSearchStrategy exists, Qdrant status checks, vector index cron jobs documented). However, RRF (Reciprocal Rank F

### Already present in PSIS (was a false positive — no work)
- Search suggestion (Did you mean) — ahgDisplayPlugin/lib/Services/FuzzySearchService.php, ahgDisplayPlugin/modules/d
- Advanced search form with filter UI — ahgDisplayPlugin/modules/display/actions/actions.class.php (lines 52-150 for fil
- Faceted search with 6 PSIS-parity facets — ahgDisplayPlugin/lib/Services/DynamicFacetService.php, ahgDisplayPlugin/modules/
- Cursor-based pagination — ahgGraphQLPlugin/lib/GraphQL/Schema/Types/ScalarTypes.php (encodeCursor/decodeCu
- Geospatial search filtering — ahgGISPlugin/lib/Services/SpatialSearchService.php (boundingBox, radius methods)
- SRU server endpoint controller — ahgLibraryPlugin/lib/Service/SruService.class.php, ahgLibraryPlugin/modules/z395
- PageIndex LLM hierarchical tree extraction — ahgDiscoveryPlugin/modules/discovery/actions/actions.class.php (executePageindex

Full report: docs/HERATIO_PSIS_PARITY_AUDIT.md + corrected verdicts in this session's log.