**CORRECTED** (cross-plugin re-verification, 2026-06-01). The first same-name audit over-counted; this reflects the true gap.

**Domain:** iiif-3d — verified: **4 missing, 4 partial, 5 already-present (false positives dropped).**

### Genuinely missing
- **IIIF Change Discovery 1.0 (Activity Streams)** — No ChangeDiscoveryController, IiifChangeDiscoveryService, or ActivityStream implementation found. No Activity Streams 2.0 compliant endpoints detected in codeba
- **SHACL Validation (RiC-O Shapes)** — No ShaclValidationService or RiC-O SHACL shape validation implementation found. RIC data integrity relies on SPARQL queries but no formal SHACL constraint check
- **RDF Import with Dry-run (TTL/JSON-LD/RDF-XML)** — No RdfImportService, RdfImportController, or import workflow found. RiC data is generated via extractor and loaded to Fuseki externally, not imported through At
- **SPARQL Proxy (Read-only Federated Queries)** — No SparqlProxyController or proxy service for read-only federated SPARQL queries found. RicSyncService queries directly to configured Fuseki endpoint but no pub

### Partial (real sub-gap remains)
- OCR Export (TXT, ALTO, hOCR, PAGE-XML) — Database table iiif_ocr_text exists with format column supporting 'plain, alto, hocr' but no OcrExportService or export controller found. Table structure is pre
- Mirador Workspace Persistence (Issue #699) — Table iiif_saved_view defined in migration file with token, state_json, click_count columns. Referenced in iiifContent actions index method querying saved views
- NER -> IIIF Annotation Surface (Issue #697) — NER pipeline exists in ahgAuthorityPlugin creating entities but no explicit controller or service for converting NER results to IIIF Annotation bodies. IiifAnno
- RiC Entity CRUD + SPARQL Query Service — RicSyncService provides entity sync to Fuseki with SPARQL query execution (executeSparqlQuery method). ricExplorer buildGraphFromDatabase reads records/actors/s

### Already present in PSIS (was a false positive — no work)
- IIIF Content State (RFC 8947) + Auth 2.0 Flow — ahgIiifPlugin/lib/Services/ContentStateService.php, ahgIiifPlugin/lib/Services/I
- IIIF Metadata Enrichment (IPTC/EXIF/XMP -> Manifest) — ahgIiifPlugin/lib/Services/IiifManifestV3Service.php
- TripoSR 2D->3D Auto-configuration — ahg3DModelPlugin/lib/Services/TripoSRService.php, ahg3DModelPlugin/lib/Services/
- RiC Serialization (full RIC-O JSON-LD -> Turtle) — ahgMetadataExportPlugin/lib/Exporters/RicoExporter.php, atom-framework/src/Servi
- CIDOC-CRM v7.1.3 Per-Record Export (Issue #659) — ahgMuseumPlugin/lib/ahgCIDOCExportService.class.php, ahgMuseumPlugin/modules/cid

Full report: docs/HERATIO_PSIS_PARITY_AUDIT.md + corrected verdicts in this session's log.