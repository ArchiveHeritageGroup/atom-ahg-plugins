<?php

class DigitalObjectShow3DComponent extends AhgComponents
{
    public function execute($request)
    {
        // $this->resource is set by get_component() via the var holder.
        // The _show3D.php template reads $resource->path and $resource->name directly.
    }
}
