/**
 * Updated showResults function with cover fallback handling.
 * 
 * Replace the cover image section in showResults() with this:
 */

        // Cover image with fallback chain
        if (preview.cover_url || data.raw?.isbn_13 || data.raw?.isbn_10) {
            const isbn = data.raw?.isbn_13 || data.raw?.isbn_10 || '';
            const openLibraryCover = `https://covers.openlibrary.org/b/isbn/${isbn}-M.jpg`;
            const fallbackCover = preview.cover_url_fallback || '/plugins/ahgThemeB5Plugin/images/no-cover.png';
            
            html += `
                <div class="col-md-3 text-center">
                    <img src="${escapeHtml(openLibraryCover)}" 
                         alt="Cover" 
                         class="img-fluid rounded shadow-sm book-cover-preview" 
                         style="max-height: 200px;"
                         loading="lazy"
                         onerror="this.onerror=null; this.src='${escapeHtml(fallbackCover)}';">
                    <div class="mt-2">
                        <small class="text-muted">
                            <i class="fas fa-image me-1"></i>Open Library
                        </small>
                    </div>
                </div>
            `;
        }
